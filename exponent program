class Exponent {
    public static void main(String[] args) {
        double x = Double.parseDouble(args[0]);
        double y = Double.parseDouble(args[1]);

        double power = Math.pow(x, y);
        long result = Math.round(power);

        System.out.println("Result = " + result);
    }
}
